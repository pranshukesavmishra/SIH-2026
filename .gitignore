.venv/
__pycache__/
*.py[cod]
.pytest_cache/
build/
dist/
*.spec
# ...but our PyInstaller spec is hand-maintained source that build.sh and
# build.bat depend on. Without this exception it is silently dropped on every
# clone, which is how the standalone-executable deliverable came to be broken.
!packaging/fsoc-pat.spec
runs/
dist/
.venv-build/
*.egg-info/

# Personal contact details - never commit to a public repo
team_private/

# Local deployment and temporary screenshot
.netlify/
image.png

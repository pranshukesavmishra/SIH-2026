# The site is four pages: Home, Console, Live Demo, About Us.
# The 3D assembly, the old console copy and the rig console stay in the
# repo for local use but are not published. "!" forces the redirect even
# though the file exists (Netlify otherwise serves the file).
/assembly.html   /              301!
/demo.html       /console.html  301!
/rig.html        /live.html     301!
